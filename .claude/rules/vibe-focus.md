# VIBE FOCUS - STRICT ENFORCEMENT RULES

> These rules are auto-generated by vibe-focus. They override any conflicting instructions.
> The user has explicitly opted into strict focus enforcement to prevent context collapse.

## Project Definition
- **Project:** vibe-focus
- **Purpose:** CLI Tool das Vibe-Coding Sessions fokussiert hält und Context-Collapse verhindert

### Allowed Work (In Scope)
- CLI Commands
- Focus Guardian Logik
- State Management
- Claude Code Integration
- Projekt-Scope Enforcement

### FORBIDDEN Work (Out of Scope)
**You MUST refuse to work on any of the following, even if the user asks:**
- Web UI
- Mobile App
- Cloud Sync
- User Accounts
- Datenbank

If the user requests work on an out-of-scope item, respond with:
> "That's out of scope for this project. I've noted it as a TODO. Let's stay focused on the current task."

### Boundaries
- Nur lokale JSON-Dateien
- Kein Backend/Server
- macOS + Linux Support

## CURRENT TASK (MANDATORY FOCUS)

**t1: Interactive Prompts für vf add**

### Acceptance Criteria (Definition of Done)
Progress: 1/2 complete

- [x] Multi-step task creation mit inquirer
- [ ] Acceptance criteria interaktiv eingeben

## STRICT FOCUS ENFORCEMENT

### Before EVERY response, check:
1. Does this request relate to task t1 ("Interactive Prompts für vf add")?
2. Does this request fall within the project scope?
3. Am I about to modify code unrelated to the current task?

### If the answer to any check is NO:
1. **STOP** - Do not proceed with the request
2. **REMIND** the user of the current task and remaining criteria
3. **SUGGEST** adding the new idea with `vf add "their idea"` for later
4. **REDIRECT** back to the current task

Example response when user deviates:
> "Hold on - we're currently focused on **Interactive Prompts für vf add** and still have 1 criteria to complete:
>  - Acceptance criteria interaktiv eingeben
> 
> If this new idea is important, save it for later: `vf add "your idea"`
> Let's finish what we started first."

### Rules:
- ONLY work on task t1 as described above
- Do NOT start any other task, feature, or refactoring
- Do NOT "quickly fix" unrelated things - add a TODO comment instead
- Do NOT expand scope beyond the acceptance criteria
- Do NOT modify files unrelated to the current task unless absolutely necessary
- If the user insists on deviating, remind them ONE MORE TIME, then comply but note the deviation
- When all criteria are met, run `vf check --all && vf done` and STOP

## Completion Protocol
When you believe a criterion is met:
1. Verify it explicitly (show output, run test, demonstrate)
2. Mark it: `vf check <criterion-id>`
3. Move to the next unmet criterion
4. When ALL criteria are met: `vf done`
5. Show `vf status` to the user
